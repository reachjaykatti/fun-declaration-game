<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  /css/styles.css
  /js/global.js</script>
</head>

<body data-theme="<%= (typeof theme !== 'undefined' ? theme : '') %>"
      data-fd-home-url="<%= currentUser ? '/dashboard' : '/login' %>"
      data-prev-url="<%= prevUrl || '' %>">

<nav class="top-nav">
  <div class="nav-left">
    #">← Back</a>
    <span class="brand">✈️ Travel Plan</span>
  </div>

  <div class="nav-right">
    <% if (currentUser) { %>
      /dashboardDashboard</a>
      /seriesMy Series</a>
      <% if (currentUser.is_admin) { %>
        /adminAdmin</a>
      <% } %>
      /logoutLogout (<%= currentUser.display_name %>)</button>
      </form>
    <% } else { %>
      /loginLogin</a>
    <% } %>

    <button class="btn btn-outline js-theme-toggle">🌓 Theme</button>
  </div>
</nav>

<main class="container">
